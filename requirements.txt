# Environnement de développement complet (toutes couches)
# Pour un usage isolé par service, voir les requirements.txt dans chaque dossier.

-r dlt_pipeline/requirements.txt
-r dbt_project/requirements.txt
-r dagster_project/requirements.txt
-r ml/requirements.txt
-r api/requirements.txt

ruff==0.6.9
pytest==8.3.3
httpx==0.27.2
